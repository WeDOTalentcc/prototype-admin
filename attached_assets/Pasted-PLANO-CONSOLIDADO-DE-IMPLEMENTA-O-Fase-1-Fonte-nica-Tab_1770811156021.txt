PLANO CONSOLIDADO DE IMPLEMENTAÇÃO

Fase 1 — Fonte Única: Tabela screening_question_sets (Foundation)

Criar tabela versionada como fonte autoritativa
Migrar dados existentes de JobVacancy.screening_questions e job_screening_questions como versão 1
Criar serviço ScreeningQuestionSetService com operações: save (auto-versiona), get_current, get_by_version, list_versions
JobVacancy.screening_questions vira cache de leitura (atualizado automaticamente ao salvar nova versão)
Fase 2 — Unificar Wizard com Pipeline WSI

Substituir suggest_screening_questions() no nó screening_collector do Wizard pelo wsi_screening_pipeline.build_pipeline()
Mapear dados já coletados no Wizard (competências, skills, senioridade) para o WSIScreeningPipelineRequest
Deprecar schema ScreeningQuestion em favor de UnifiedScreeningQuestion
Salvar resultado na screening_question_sets com source="wizard"
Garantir que ao abrir o modal depois do Wizard, as perguntas carregam da versão vigente (sem regenerar)
Fase 3 — Vincular Triagens à Versão do Roteiro

Adicionar question_set_version_id em wsi_sessions
Ao disparar triagem (Deepgram ou OpenMic), buscar perguntas da versão vigente em screening_question_sets (não regenerar)
Copiar para wsi_questions como snapshot do que o candidato recebeu
Funciona igual para ambos os canais
Fase 4 — Alerta de Roteiro Alterado

Ao salvar nova versão, verificar candidatos já triados com versão anterior
Endpoint de verificação de consistência
Frontend: badge no modal e lista de candidatos
Fase 5 — Normalização de Scores e Comparação Justa

Coeficiente de dificuldade por versão (baseado em Bloom/Dreyfus médio)
Atualizar candidate_comparison_service para normalizar antes de comparar
Flag same_question_set no resultado
Fase 6 — Re-triagem e Progress Bar

Endpoint para re-triagem com novo roteiro
Histórico de sessões (anterior marcada como superseded)
Progress bar inteligente: resetar ao editar, estado visual diferente //////.    Confirmado. O pipeline WSI já faz isso:

Bloco 2: Busca perguntas padrão da empresa via company_questions_raw → _build_company_block() → normaliza para UnifiedScreeningQuestion com block_id=2, Bloom nível 2, Dreyfus estágio 2, peso 0.9 (eliminatórias) ou 0.7 (informativas)

Bloco 3: Elegibilidade WSI (disponibilidade, modelo de trabalho, salário) — com deduplicação contra Bloco 2 para não repetir

Bloco 4: Técnicas (geradas com base em skills e senioridade)

Bloco 5: Comportamentais/Situacionais (Big Five, CBI)

Então sim — quando o Wizard chamar o build_pipeline(), ele precisa passar o parâmetro company_questions_raw (buscado da configuração da empresa) e include_company_questions=True no request. O pipeline já cuida de:

Normalizar as perguntas da empresa para o formato UnifiedScreeningQuestion
Deduplicar contra os outros blocos
Distribuir proporcionalmente por senioridade
Atribuir Bloom/Dreyfus adequados
O Wizard hoje não faz nada disso — ele gera 3-4 perguntas genéricas sem classificação WSI. Ao migrar para o pipeline, ele passa a produzir o roteiro completo e padronizado, idêntico ao que o modal gera.

Um detalhe importante: No contexto do Wizard, o recrutador está conversando com a LIA e pode não ter todas as informações prontas no momento exato do screening_collector. A abordagem seria:

Se já tem skills/competências coletadas nas etapas anteriores → gera o roteiro completo via pipeline
Se ainda faltam dados → a LIA pergunta conversacionalmente o que precisa antes de gerar
O recrutador pode depois refinar no modal (que carrega da mesma fonte versionada)
Quer que eu inicie a implementação pela Fase 1?
