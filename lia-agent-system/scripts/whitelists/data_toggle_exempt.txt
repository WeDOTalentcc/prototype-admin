# UI-only data-toggle attributes exempt from check_data_toggle_matches_persisted_key.
#
# Format: <repo-relative-path>:<toggle-name>:<short-reason>
# - path is relative to workspace root (e.g. plataforma-lia/src/...)
# - lines starting with `#` and blank lines are ignored
#
# Use this file (instead of inline `// DATA-TOGGLE-EXEMPT` comment) when the
# data-toggle is inside a JSX/TSX expression where adding a trailing line
# comment would be awkward or get re-formatted by Prettier.
#
# Each exemption MUST document WHY the toggle is legitimate UI-only (no
# persistence) so future maintainers can audit:
#  * pure derived state (e.g. computed from another field)
#  * UI-namespaced label whose payload uses the canonical backend name
#    via a different state shape
#  * client-side ephemeral state (e.g. expand/collapse, modal open)
#
# Audit canonical: vide CLAUDE.md "lia_field_toggles canonical pattern".
#
# ─────────────────────────────────────────────────────────────────────────
# Entries
# ─────────────────────────────────────────────────────────────────────────

# UI label namespaced to the screening editor; the actual persisted payload
# uses `is_required` (see useRecruitmentScreening.ts:97 mapping
# `is_required: q.required`) which matches `screening_question.is_required`
# column. The `data-toggle` literal serves as a stable UI test handle and
# is intentionally scoped with `question_` prefix.
plataforma-lia/src/components/settings/RecruitmentScreeningTab.tsx:question_required:ui-namespaced-label-payload-uses-is_required

# Same rationale as above: payload uses `is_eliminatory` (canonical
# column `screening_question.is_eliminatory`). UI label is namespaced.
plataforma-lia/src/components/settings/RecruitmentScreeningTab.tsx:question_is_eliminatory:ui-namespaced-label-payload-uses-is_eliminatory

# Pure UI-derived state from `role === \"admin\"` (see
# use-user-management.ts:77). Never persisted; only `role` is sent to the
# backend on POST/PUT user. The form toggle is a UX shortcut that flips
# `role` between admin and the previous value.
plataforma-lia/src/components/settings/user-form.tsx:isManager:ui-derived-from-role
