# Allowlist for scripts/check_es_search_has_tenant_filter.py (Task #1147).
#
# Format: one entry per line, either:
#   <relative/path/to/file.py>            (allow ALL es.search/es.count in this file)
#   <relative/path/to/file.py>:<lineno>   (allow a specific callsite)
#
# Lines starting with '#' and blank lines are ignored.
#
# An entry MUST justify itself with a comment on the previous line explaining
# WHY the callsite is exempt (health probe, admin cross-tenant audit, etc.).
# Entries without justification will be rejected at code-review time.
