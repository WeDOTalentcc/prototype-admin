[project]
name = "lia-llm"
version = "0.1.0"
description = "LLM tier routing, token budget, model config — WeDOTalent"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lia_llm"]
