# Overview
Plataforma LIA (Learning Intelligence Assistant) is an AI-powered recruitment and selection system designed to optimize hiring processes. It leverages a multi-agent AI architecture and the WSI (WeDoTalent Skill Index) methodology to provide comprehensive candidate assessment (technical, behavioral, cultural fit). LIA aims to revolutionize recruitment by offering advanced AI capabilities, robust authentication, candidate comparison, specialized testing, KPI dashboards, real-time notifications, and predictive analytics, ensuring an efficient, compliant, and intelligent solution that improves hiring quality and reduces time-to-hire. The platform covers the entire hiring cycle from attracting to hiring and analysis, with a focus on intelligent automation and detailed reporting.

# User Preferences
- Idioma: Português
- Design/Layout: Sempre perguntar antes de fazer mudanças em design ou layouts - o usuário quer avaliar propostas antes da implementação
- Separação Backend/Frontend: Manter estruturas de back e front completamente separadas (API REST/GraphQL no backend, SPA no frontend)
- Componentização: Priorizar componentes reutilizáveis e modulares, evitar código monolítico
- Preparação para Migração: Estruturar código pensando em possível conversão para Vue.js + Nuxt (frontend) e Ruby on Rails (backend)
- Border Radius: Seguir DS v4.2.2 — rounded-md (8px) padrão universal para botões/inputs/cards/modais, rounded-xl (16px) para interfaces imersivas (chat, login), rounded-full (pill) para badges/skills/avatars.
- Chat é a interface principal - O recrutador interage com a LIA através de conversa natural, NÃO através de botões
- LIA pergunta, recrutador responde - Quando uma etapa está completa, a LIA PERGUNTA se quer avançar. O recrutador RESPONDE no chat (ex: "sim", "vamos", "pode avançar")
- Painéis são suporte visual - Os painéis laterais mostram informações e permitem edição, mas a navegação e decisões são feitas via chat
- Sem botões como interface principal - Botões são apenas atalhos opcionais, NUNCA a forma principal de interação
- Transições via confirmação textual - O recrutador confirma avanço de etapa escrevendo no chat, não clicando em botões
- A LIA deve entender variações naturais de confirmação em português

# System Architecture
The platform uses Next.js, React, and TypeScript for the frontend, styled with Radix UI, shadcn/ui, and Tailwind CSS. The backend is built with FastAPI (Python) and PostgreSQL. AI agents are orchestrated with LangGraph and primarily powered by Claude Sonnet.

**Core Architectural Decisions & Features:**
- **Multi-Agent AI System**: Orchestrates specialized AI agents using the WSI methodology for candidate evaluation and supports a custom agent marketplace with metering and billing. This system uses a CascadedRouter with 8 tiers and 19 canonical domains, managing 27 agents and tool permissions per tenant. **Severity-based delegation** (`_decide_agent_type_from_hints` em `app/orchestrator/main_orchestrator.py`): hints proativos do `PreConditionChecker` só desviam o turno para `company_settings` quando severity é `warning`/`critical` — hints `info` (benefits/culture/policy ausentes) ficam apenas como sugestão anexada ao prompt e a intenção primária do usuário (ex.: criar vaga) é executada normalmente.
- **Intelligent Conversational Interface (LIA)**: The primary interaction is chat-based, supporting multi-step reasoning, intent classification, and session persistence. It includes a unified chat system with WebSocket and SSE fallback. It supports a "5-Chat + 2-Channel Architecture" (Job Creation, Talent, Jobs Management, Pipeline/Kanban, and Policy chats, integrated with WhatsApp and MS Teams).
- **UI/UX Design**: Adheres to an "ElevenLabs pattern" with a monochromatic palette, cyan accents, a 3-font system, interactive pipeline flows, sortable columns, advanced pagination, and a command palette, all aligned with Design System v4.2.2. It features dynamic split-screen panels and a floating Super Prompt, with a redesigned sidebar and integrated TipTap Rich Text Editor.
- **WSI Screening**: A 6-block AI-powered methodology for comprehensive candidate evaluation, generating detailed reports with STAR analysis, Bloom/Dreyfus levels, gap analysis, and recommendations. Includes a public candidate-facing chat page for WSI screening with text and bidirectional audio support. WSI scoring has been scaled from 0-5 to 0-10 with corresponding adjustments to calculations and thresholds.
- **Compliance (3-Pillar Architecture)**: Designed to comply with LGPD, SOX, and EU AI Act, incorporating FairnessGuard and FactChecker for bias detection. Includes a `BiasAuditService` to calculate adverse impact. Various compliance shields (FairnessGuard 3L, PII Masking 4L, FactChecker, BiasAudit Four-Fifths, AuditTrail SOX, Policy Engine, PromptInjectionGuard, C3B Layer, Scoring Safeguards) are integrated.
- **Job Creation & Candidate Management**: Supports conversational and manual job creation, with a Kanban candidate movement system featuring AI-powered sub-status prediction and WSI score integration. A Job Readiness Hub provides a 7-stage visual pipeline for preparing imported ATS vacancies.
- **Pipeline / Recruitment Stages — 4 endpoints distintos (Audit Configurações Fase 3 — D-2)**: o sistema expõe 4 conceitos diferentes que parecem sobrepostos mas são canonicamente distintos:
  1. **`/api/v1/company-pipeline`** (frontend: `useRecruitmentPipeline.ts`) — pipeline canônico DA EMPRESA (1 fonte de verdade por tenant). PUT salva os stages padrão. Consumido pelo hub Pipeline em Configurações.
  2. **`/api/v1/recruitment-stages/*`** (frontend: `use-recruitment-stages.ts`) — stages RESOLVIDOS POR VAGA (template + override do recrutador) + sub-status + screening questions. CRUD/transition. Consumido pelo Kanban e EditJob.
  3. **`/api/v1/pipeline-policy/*`** (`pipeline_policy.py`) — VALIDAÇÃO de transições no Kanban (não CRUD). Resolução de templates por tipo de vaga.
  4. **`/api/v1/company/pipeline-templates/*`** (frontend: `useEditJob.ts`) — CATÁLOGO de templates pré-prontos (Tech, Sales, Healthcare etc.) com `seed-defaults` e `increment-usage`.
  Decisão canonical: **manter os 4 separados**. Não há duplicação semântica — cada endpoint serve um caso de uso distinto. Migração para um único endpoint quebraria a separação de responsabilidades (template vs instância vs política vs definição).
- **Network Policy — `apiFetch` wrapper (Audit Configurações Fase 3 — D-4)**: todas as chamadas autenticadas ao backend via `/api/backend-proxy/*` devem usar `apiFetch` de `@/lib/api/api-fetch.ts`. O wrapper injeta `credentials: 'include'` explícito, documentando intenção de segurança e prevenindo regressões silenciosas em deploys cross-origin (preview branches, mobile webview, multi-domínio). Migrados 78 fetches em 26 arquivos do menu Configurações na Task #927.
- **Canonical Job-Creation Wizard (Task #850)**: `app.domains.job_creation.graph.JobCreationGraph` is the **sole** backend entry point for conversational job creation. The legacy stack (`wizard_react_agent`, `job_wizard_graph`, `wizard_smart_orchestrator`, `app/tools/job_wizard_tools.py`, `app/domains/job_management/tools/job_wizard_tools.py`, `stage_context`, `wizard_system_prompt`, `wizard_tool_registry`, `feature_flag.py`) was deleted and is no longer registered in `langgraph.json`, `agents_registry.yaml`, or `react_agent_registry.py`. The legacy `GraphRunnerService.run_job_wizard` API surface now transparently routes to the canonical graph instead of failing — existing callers keep working. Intake routing is handled by the canonical `app/domains/job_creation/services/intake_extractor.py` with the **Pydantic `JobIntakePayload` schema**: nested `LocationField{city,state,country}`, split `manager_name` + `manager_email`, per-field categorical `confidence_label` (`high`/`medium`/`low`) derived from the numeric score, and a provenance `source` enum (`user_text` | `right_panel_form` | `attached_file`, plus legacy `llm`/`regex`/`default`/`user`). `IntakeExtractor.extract_from_sources(user_text, right_panel_form, attached_file_text)` merges all three intake channels with priority `form > text > file` and conflict-resolution by confidence. The HITL invariant is enforced by the `NEVER_PRECOMPLETED` frozenset (`{"jd_enrichment", "wsi_questions"}`): HITL 1 (recruiter approves enriched JD) and HITL 2 (recruiter approves WSI questions) **always run**, even when the recruiter provides full intake up-front. `_STAGE_REQUIREMENTS` covers `intake`, `salary`, `competency` and `bigfive` precompletion. The autonomous-agent handlers `_wrap_validate_job_requirements` and `_wrap_get_company_config` were re-wired to the canonical IntakeExtractor and `CompanyConfigurationService` respectively (no more `not_implemented` payloads). Health probe `/api/v1/health/langgraph` reports `JobCreationGraph` alongside `InterviewGraph` and `WSIInterviewGraph`. Frontend rewrites are deferred to a follow-up task. Tests: `lia-agent-system/tests/unit/test_intake_extractor.py` — **38 cases** covering payload primitives, NEVER_PRECOMPLETED HITL invariant, regex fallback, LLM-unavailable path, LLM-success path with the new split-manager schema, confidence-label buckets, structured location + manager split, multi-source merge at the service layer (form-beats-text, file-fills-gaps, HITL preserved across sources), in-graph multi-source wiring (`intake_node` routes to `extract_from_sources` when `right_panel_form` or `attached_file_text` is present and to `extract(query)` for text-only intake), the legacy `run_job_wizard` compatibility-shim contract (non-empty `response_text` translated from `ws_stage_payload`, `job_draft` from `intake_payload`, `reasoning_steps` from `stage_history`), the WS HITL resume path (`agent_chat_ws._resume_wizard_canonical` calls `JobCreationGraph.resume(thread_id, prior_state, updates)` with prior state pulled from the checkpointer + merged approval payload, and extracts the recruiter-facing message from `ws_stage_payload.data.message` with stage-aware fallback — never from legacy `response`/`user_message` keys), and the retirement guard on `stream_job_wizard()` (raises `NotImplementedError` with a migration message instead of silently dispatching to the removed legacy graph). The WS handler also forwards the canonical `ws_stage_payload` to the UI alongside the assistant message so the right-panel form stays in sync. Confidence aggregation in `_llm_extract` iterates `payload.model_fields` dynamically so new schema fields are picked up automatically. The retired Celery tasks `agents.wizard.process_async` and `agents.wizard.execute` are no longer registered (and removed from `DOMAIN_QUEUES`); wizard remains in `SYNC_DOMAINS` and runs synchronously through the WS layer.
- **Closed-Loop Action Execution**: Enables full closed-loop actions across chats using `ActionExecutorService` with LLM-powered smart parameter extraction.
- **Talent Funnel Search Optimization**: Leverages Elasticsearch, PG Vector, and WRF for advanced search with LLM job classification and candidate scoring.
- **Progressive Automation & CompanyHiringPolicy**: Implements `CompanyHiringPolicy` to control automation levels with a confidence-based decision engine and conversational onboarding.
- **Broker Abstraction Layer**: Utilizes a `BrokerInterface` for messaging with `RedisBroker`, `RabbitMQBroker`, and `PubSubBroker` implementations.
- **Voice Analysis Integration**: VoIP browser calls use Gemini Live Audio API with Twilio as PSTN fallback.
- **Security & Production Readiness**: Includes robust authentication with httpOnly cookies, JWT, and WorkOS SSO, API security measures, unified health endpoint, structured logging, and global exception handlers. RLS (Row Level Security) is enforced at the PostgreSQL level.
- **Talent Intelligence Domain**: Implements competitive capabilities such as a Skills Ontology Engine, Internal Mobility matching, Workforce Planning, Interview Intelligence, and Passive Candidate Nurture.
- **Interview Intelligence Infrastructure**: Full interview lifecycle with Microsoft Calendar integration, Gemini-based audio/video transcription, and 5 services for WSI methodology, bias detection, comparative analysis, strategic opinion, and feedback generation.
- **External API Consumption Tracking**: Unified ledger for tracking external API costs per tenant from services like Apify and Pearch, with budget alerts.
- **ATS Integration**: Full integration with Gupy, Pandapé, and Merge.dev.
- **Monetizable Modules Infrastructure**: Provides a framework for managing and gating features as modules with different tiers and statuses (beta, trial, active, expired).
- **LLM Configuration**: Allows per-tenant configuration of LLM providers (Gemini, Claude, OpenAI).
- **Recursive RAG Chunking**: Implements a `RecursiveTextSplitter` strategy for hierarchical document chunking.
- **CrewAI-Style Delegation on AgentBus**: A formal multi-agent delegation system built on AgentBus with `AgentCrew`, `CrewPlan`, and `CrewPlanExecutor` for task orchestration.
- **Database Migrations**: Uses Alembic, with versions managed in `lia-agent-system/alembic/versions/`. Migrations are automated with `scripts/post-merge.sh` and ensure schema consistency.
- **Demo Tenant Seed Estendido (Task #813)**: `app/shared/services/seed_service.py` expõe `seed_demo_company_settings(db)`, função idempotente (skip-if-exists) e restrita ao tenant demo (`DEMO_COMPANY_UUID`) que popula 8 tabelas críticas — `company_benefits` (25 BR padrão), `company_culture_profiles`, `company_hiring_policies`, `company_compliance_controls` (5 controles × 3 frameworks), `company_responsibilities`, `company_skills_catalog` + `behavioral_competencies_catalog`, `company_retention_policies`, `company_screening_questions`. Garante a row canônica em `company_profiles` (id = DEMO_COMPANY_UUID) para satisfazer FK de culture profile. Após esse seed, os hints `benefits_catalog_empty`, `culture_profile_missing` e `hiring_policy_missing` do `PreConditionChecker` passam a retornar False para o tenant demo. Plugado dentro de `seed_demo_data` no entry point `/api/v1/admin/seed-data/demo`.
- **Auditoria Canônica do Chat Runtime (Task #817)**: `docs/audit/chat-runtime-audit.md` é o relatório (PT-BR) que mapeia 3 sintomas runtime do chat e suas correções canônicas. (1) **WS-token canônico**: `src/hooks/chat/useChatSocket.ts` é o ÚNICO consumidor produtivo de `/api/auth/ws-token` (já com retry 3× backoff 1500ms). Removidos 2 hooks de código morto (`src/hooks/use-float-streaming.ts` + `src/hooks/ai/use-float-streaming.ts`, ~675 linhas, 0 consumidores) + teste órfão. (2) **Pipeline-pulse defesa em profundidade**: consumer `chat-workflow-reels.tsx` agora valida `Array.isArray(data?.stages)` antes de iterar; proxy `src/app/api/backend-proxy/pipeline-pulse/route.ts` valida shape com `isValidPulsePayload` e retorna 502 explícito se backend devolver payload divergente em 200 OK. (3) **Intent classifier — falso positivo desmontado**: existem 2 classificadores legítimos (`IntentType` 5 valores UPPERCASE para wizard / `EnhancedIntentType` 10 valores UPPERCASE para chat geral) + 2 shims intencionais em `app/shared/services/` re-exportando o canônico em `app/domains/ai/services/` + 2 conceitos não-relacionados que apenas compartilham "intent" no nome. A constante `_COMPANY_SETTINGS_INTENTS` (`main_orchestrator.py:95-99`) **EXISTE** e é intencional (Task #811 severity-based delegation): contém 4 valores LOWERCASE (`{"company_settings", "configure_company", "settings_config", "hiring_policy"}`) que **não colidem** com a saída dos classificadores após `.lower()` — invariante uppercase/lowercase preserva safety, congelada em teste. Testes regressivos: `tests/unit/test_intent_classifier_no_company_settings.py` (8 cenários cobrindo invariantes de enum, identidade de shims, contrato `_COMPANY_SETTINGS_INTENTS` e `_decide_agent_type_from_hints` para CREATE_JOB/UPDATE_FIELD/QUESTION/empty + cenários de hijack legítimo via ctx.intent + warning severity) + `chat-workflow-reels-pulse.test.ts` (9 cenários) + `pipeline-pulse/route.test.ts` (validador 7 cenários + handler integrado 5 cenários) + `ws-token/route.test.ts` (4 modos: jwt-cookie, workos, dev-auto-login, no_credentials). 3 ADRs registrados.
- **Wizard JD Upload — Async Progress (Task #865)**: O upload de JD agora é assíncrono ponta a ponta. O proxy `src/app/api/backend-proxy/jd-import/upload/route.ts` continua repassando 202+`task_id` do worker `lia-agent-system/app/jobs/tasks/jd_upload.py`, mas o front passou a consumir os eventos `background_task_update` (status `queued` → `running` → `completed`/`failed`) emitidos pelo Celery. O hook `src/components/unified-chat/useJdUploadProgress.ts` é o único caller produtivo do proxy: faz `POST` com `?session_id=<chatSessionId>&consent_acknowledged=true`, semeia uma linha `queued` em `chatBackgroundTasks` via `seedBackgroundTask` (novo callback exposto por `useChatSocket` → `useLiaChatConnection` → `useLiaChatContext`) e observa o store; ao `completed` dispara `lia:prefill-message` carregando `imported_jd_id` para o wizard intake; ao `failed` injeta uma mensagem de chat com a mensagem do worker **verbatim** (`fairness_blocked` / `empty_text` / `resource_exceeded` / `payload_expired`). `useWizardIntegration` foi simplificado para não disparar mais o "Criar vaga…" automaticamente em `lia:file-upload-confirmed` do tipo JD — esse fluxo agora é orquestrado pelo hook acima. Os tipos `BackgroundTaskEvent`/`BackgroundTask` ganharam `status:"queued"` e `task_type:"wizard"`; `BackgroundAgentsStatus` mostra ícone `FileUp` + spinner cinza para `queued`. Cobertura: `src/components/unified-chat/__tests__/useJdUploadProgress.test.tsx` (5 cenários — happy path, fairness/failed verbatim, proxy 401, branch CV ignorada, fallback síncrono 200).
- **Configurações: limpeza Lote 1 — cluster Goals (Task #897)**: Removidos 18 arquivos órfãos do diretório `plataforma-lia/src/components/settings/` (~3.000+ LoC). 12 arquivos do cluster `Goals/Workforce planning` foram deletados conforme escopo da task: `GoalsPlanningHub.tsx`, `GoalsAlertsSection.tsx`, `GoalsWorkforceSection.tsx`, `GoalsFormModals.tsx`, `GoalsTemplateSelectModal.tsx`, `GoalsTemplatesModal.tsx`, `goals-templates-modal.tsx`, `goals-edit-modals.tsx`, `goals-custom-goal-modal.tsx`, `goals-management.tsx`, `use-goals-management.ts`, `goals-management-types.ts`. Também deletada por consequência (dead-by-cascade) a pasta `goals/` inteira (6 arquivos: `index.ts`, `goals-utils.ts`, `EditableCell.tsx`, `DeleteConfirmModal.tsx`, `GoalsStatsCards.tsx`, `ApplyAllModal.tsx`) — todos consumidos exclusivamente pelos arquivos do cluster removido. Desvio do escopo original: `useGoalsPlanningHub.ts` foi **mantido** porque tem 3 importadores vivos no menu (`WorkforceHubContent.tsx` → `MinhaEmpresaCard.tsx` → `MinhaEmpresaHub.tsx` → `settings-page-enhanced.tsx`, além de `WorkforceSection.tsx` e `AlertsSection.tsx`). Uma refatoração desse hook é candidata a um lote futuro (junto com `goalsPlanningConstants.ts`, `WorkforceHubContent`, `WorkforceSection`, `AlertsSection`). Verificado: `tsc --noEmit` não introduziu nenhum erro novo (único erro remanescente é pré-existente e sem relação: `src/hooks/shared/use-teams-tab-tracker.ts:22` — sintaxe inválida de baseline).
- **Configurações Fase 3 — Cobertura de testes backend 24/24 (Task #930)**: Cobertura pytest do menu Configurações elevada de 14/24 → **24/24 endpoints (100%)**. Escopo: **apenas criar** 9 arquivos em `lia-agent-system/tests/api/v1/test_*_coverage.py`, **sem editar produção** em `app/api/v1/`. Tripé por endpoint = happy + erro + isolamento (a forma do isolamento varia conforme o guard real — para endpoints que derivam `company_id` de `get_verified_company_id`/`get_current_user`, o teste alterna COMPANY_A ↔ COMPANY_B via `dependency_overrides` e captura o `company_id` repassado ao repo/serviço, validando que o filtro de tenant segue o sujeito autenticado e nunca um parâmetro arbitrário do client; para `integrations_hub`, há ainda 403 explícito de `verify_connection_ownership`). Distribuição final: `settings_progress` (3), `pipeline_policy` (3), `alerts` config + preferences + list (15 — `GET /config`, `PUT /config`, `GET /preferences`, `POST /preferences`, `PUT /preferences`, `GET /alerts/`), `webhooks` events + POST create + GET list + PATCH update + DELETE + POST test (15), `fairness_reports` summary + trend + audit (9), `integrations_hub` providers + connections (11, inclui 403 cross-tenant real), `integrations` Teams (9), `llm_config` GET + PUT + test + providers (9, com mock de `LlmConfigRepository` para evitar dependência real de `ENCRYPTION_KEY` em `lia_config.security.encryption`), `stage_transition_automation` predict + bulk + substatus-options (8). Endpoints fora do escopo do menu Configurações (POST /generate-message, /regenerate-for-substatus, /get-actions, /bulk-generate-messages em `stage_transition_automation`; POST /check, /{id}/acknowledge, /{id}/resolve, /summary em `alerts`) NÃO são alvo desta task — são consumidos por fluxos de runtime, não setup. Padrão técnico: `TestClient` + `app.dependency_overrides` (`get_current_user`/`get_current_active_user`/`get_verified_company_id`/`get_db`/repositórios) — mesmo do `tests/integration/test_tenant_scope_v1.py`. Sem mocks globais, sem secrets reais. **Validado**: `pytest tests/api/v1/test_*_coverage.py --no-cov` = **82 passed, 0 failed, 0 xfailed** (alerts 15 + pipeline_policy 3 + settings_progress 3 + webhooks 15 + fairness_reports 9 + integrations 9 + integrations_hub 11 + llm_config 9 + stage_transition_automation 8). Cobertura delta por módulo (`--cov`) não foi medida no shell pois a instrumentação carrega o resto do `app.api.v1` (timeout >60s); medição fica para o CI gate. **Bugs reais de produção descobertos durante a auditoria** (não corrigidos por escopo, propostos como follow-ups): (1) `settings_progress.py` aceita `company_id` como query param mas IGNORA o valor — sempre usa `get_default_company()` (tenant bypass se houver mais de uma empresa no DB); (2) `integrations_hub` valida tenant por `company_id` da query, não por authz vinculada ao `user_id` autenticado; (3) `stage_transition_automation` tem import de `OFFER_DECLINE_REASONS`/`REJECTION_REASONS` no top-level — quebra antes do handler se símbolo for removido em deploys futuros. Os bugs `#935` (UnboundLocalError em llm_config PUT) e `#936` (ImportError substatus-options) que esta nota mencionava em versão anterior **foram corrigidos em produção** antes desta sessão; testes passam por motivo legítimo, sem `xfail`. **Drift fora de escopo — situação final**: o auto-commit `afe709945` que iniciou esta sessão já incluiu, fora do escopo desta task, o IDOR fix em `lia-agent-system/app/api/v1/tasks.py` (`_resolve_scope_user_id` + `_is_owner_or_admin` para query `?user_id=`) e o `tests/integration/test_tasks_tenant_scope.py`. **Não foram revertidos** porque agora fazem parte de `HEAD` e a remoção causaria regressão de segurança. Permanecem em HEAD intactos.
- **Configurações: limpeza Lotes 4b/4c/4e/4f — clusters GlobalSearch + DataRequest + Recruitment legacy + Diversos (Task #918)**: Encerramento formal dos lotes restantes da Parte 4 da auditoria 28/abr/2026 (`attached_assets/audit-configuracoes-2026-04-28.md`). Inventário verificado: todos os 18 arquivos do escopo já estavam ausentes de `plataforma-lia/src/components/settings/` no início da task (provavelmente removidos em PRs anteriores ainda não documentados). Lista validada por `find` + `rg --no-ignore-vcs` em todo o `src/`: **Cluster GlobalSearch** — `GlobalSearchHub.tsx`, `GlobalSearchCostsTab.tsx`, `GlobalSearchLimitsTab.tsx`, `GlobalSearchOptionsTab.tsx`, `useGlobalSearchHub.ts` (ausentes; o homônimo `useGlobalSearchSettings.ts` em `src/hooks/search/` permanece VIVO, consumido por 7 calls em `useChatSession`, `useExpandableAIPromptCore`, `useSearchState`, `useCandidatesPageCore`, `smart-search-input`, `usePromptSearchState`, `use-search-source`); **Cluster DataRequest** — `DataRequestTab.tsx`, `DataRequestCollectionSection.tsx`, `DataRequestFieldsBranding.tsx`, `DataRequestFieldsBrandingSection.tsx`, `useDataRequestTabState.ts` (ausentes; LGPD agora vive integralmente em `FairnessComplianceHub > lgpd-candidatos`); **Cluster Recruitment legacy** — `OnboardingSettingsToggle.tsx`, `RecruitmentHub.tsx`, `HiringPoliciesHub.tsx` (ausentes; `useRecruitmentHub.ts`, hook compartilhado por `RecruitmentPipelineTab`/`RecruitmentScreeningTab`, é VIVO e não foi tocado); **Diversos** — `AlertsSection.tsx` (substituído por `communication-hub/AlertsTab.tsx`), `AIConfigPanel.tsx` (`AIConfigPreview.tsx` no chat permanece vivo), `CultureProfilePreview.tsx` (substituído por `company/CultureIdentitySection.tsx`), `LiaInstructionPopover.tsx`, `RecruiterPreferencesPanel.tsx` — todos ausentes. Achado lateral (não removido por estar fora do escopo literal da task): `src/components/settings/useGlobalSearchSettings.ts` (homônimo do hook vivo de `hooks/search/`) é órfão sem importadores e fica como candidato natural para o próximo lote de limpeza. Verificado: `tsc --noEmit` continua com o mesmo único erro pré-existente (`src/hooks/shared/use-teams-tab-tracker.ts:22`); nenhuma referência import de qualquer dos arquivos do escopo encontrada em `src/` (única menção residual está em `public/diagram-lia-detailed.html`, documentação estática).
- **Menu Configurações — Arquitetura Canônica (Task #903, pós Fase 2)**: `plataforma-lia/src/components/pages/settings-page-enhanced.tsx` é o **único** entry point do menu Configurações. Após a Fase 2 de canonicalização (~9.000 LoC removidas, webhooks expostos, baseline de testes instalada), o estado final é:
  - **9 hubs canônicos** (na ordem do sidebar): `minha-empresa` (MinhaEmpresaHub), `pipeline` (RecruitmentPipelineTab), `screening` (RecruitmentScreeningTab), `templates-assinatura` (CommunicationHub face A), `comunicacao-alertas` (CommunicationHub face B), `usuarios-departamentos` (UsuariosDepartamentosHub), `integrations` (IntegrationsHub), `webhooks` (WebhooksManager — exposto na T02), `fairness-compliance` (FairnessComplianceHub com 3 subsections: `fairness`/`lgpd-candidatos`/`studio`). Wrappers `PipelineStandalone`/`ScreeningStandalone`/`TemplatesAssinaturaHub` foram removidos; o switch em `renderSectionContent()` instancia diretamente as tabs canônicas. `CommunicationHub` é uma fonte única apresentada em duas faces via prop `visibleTabs`: `['templates','signature']` para `templates-assinatura` (com `stacked`) e `['schedule','alerts','abtesting']` para `comunicacao-alertas`.
  - **Mapa de eventos & deep-link** (3 caminhos para abrir uma tab):
    1. `window.dispatchEvent(new CustomEvent('settings-open-tab', { detail: '<sectionId>' }))` — string com o id do hub (alias suportado: `'alertas'` → `'comunicacao-alertas'`); ids desconhecidos são silenciosamente ignorados.
    2. `window.dispatchEvent(new CustomEvent('lia:settings-action', { detail: { section, field } }))` — bridge LIA chat → settings (Task #712); abre a tab e faz `scrollIntoView` no `[data-field="<field>"]` marcando-o com `data-recently-highlighted="true"` por 3s.
    3. Deep-link via `searchParams`: `/configuracoes?section=<sectionId>&field=<field>` é parseado por `lib/settings/resolve-settings-target.ts` (Task #894) e tem o mesmo efeito do evento 2; substituiu os antigos `?tab=` quebrados.
  - **Endpoints consumidos × módulo backend** (resumido):
    | Hub | Prefixos `/api/backend-proxy/*` consumidos | Módulo backend (`lia-agent-system/app/api/v1/`) |
    |---|---|---|
    | (root) | `settings/progress/` | `settings_progress.py` |
    | minha-empresa | `company/profile`, `company/culture-*`, `company/benefits`, `company/communication-settings`, `company/global-search-settings`, `company/ideal-profiles`, `company/pipeline-templates`, `company/retention-policy`, `big-five/profiles` | `company.py`, `company_culture*.py`, `company_benefits.py`, `communication_settings.py`, `company_retention.py`, `big_five.py` |
    | pipeline + screening | `company-pipeline`, `company/screening-questions`, `recruitment-stages/sub-statuses` | `company.py`, `pipeline_policy.py`, `stage_transition_automation.py` |
    | templates-assinatura + comunicacao-alertas | `email-templates`, `alerts/config`, `digest/weekly/preferences`, `company/communication-settings` | `email-templates`, `alerts.py`, `communication_settings.py`, `digest` |
    | usuarios-departamentos | `company/users`, `company/departments`, `company/members`, `company/approvers` | `company_users.py`, `company_departments.py`, `company_approvers.py` |
    | integrations | `integrations/status`, `calendar/*`, `ats/connections`, `llm-config` | `integrations_hub.py`, `integrations.py`, `ats.py`, `llm-config` |
    | webhooks | `webhooks/*` | `webhooks.py` (+ os 5 receivers: `mailgun_webhooks`, `merge_webhooks`, `whatsapp_webhook`, `job_status_webhooks`, `external_webhooks`) |
    | fairness-compliance | `fairness-report/summary`, `fairness/audit/logs`, `fairness-report/export`, `custom-agents/studio-compliance-summary` | `fairness_reports.py`, `bias_audit.py`, `admin_compliance_fairness.py`, `custom-agents` |
  - **Convenções obrigatórias para hubs novos**: (1) registrar via `dynamic(() => import('@/components/settings/<Hub>'), { ssr: false, loading: <LoadingFallback /> })` no topo de `settings-page-enhanced.tsx`; (2) entrada no array `getDefaultSections()` com `id`, `title`, `description`, `icon` (Lucide), `category` (`basic`/`advanced`/`integrations`), `priority` (`high`/`medium`/`low`); (3) cor do ícone declarada em `SECTION_ICON_COLORS`; (4) caso correspondente no switch `renderSectionContent()` envolto em `<ErrorBoundarySection>`; (5) `useTranslations('settings.<hub>')` é obrigatório (sem strings hardcoded PT); (6) faces múltiplas do mesmo hub canônico devem usar prop `visibleTabs` em vez de criar wrappers novos; (7) endpoints novos passam pelo proxy `app/api/backend-proxy/` (nunca chamar backend direto do client).
- **Configurações: Fase 2.5 — fechamento das pendências do audit 28/abr/2026 (sem task #, follow-up direto pós #918/#919/#923)**: Aplicadas as skills `canonical-fix`, `design-standardize` e `feature-impact` para zerar os 3 itens reais que ficaram em aberto + 3 housekeepings.
  - **T1 — hex hardcoded → tokens DS v4.2.2**: Criado `plataforma-lia/src/lib/chart-colors.ts` como fonte canônica de cores para Recharts (que não aceita classes Tailwind nas props `stroke`/`fill`). Tokens semânticos exportados: `CHART_GRID`, `CHART_AXIS`, `CHART_NEUTRAL`, `CHART_LIA`, `CHART_SUCCESS`, `CHART_WARNING`, `CHART_DANGER`, `CHART_INFO` + `CHART_PALETTE`. Migrados os 5 hex catalogados pelo audit (Dim 3 / DS): `StudioComplianceView.tsx:150,154,155` (`#e5e7eb`→`CHART_GRID`, `#60BED1`→`CHART_LIA`, `#DC2626`→`CHART_DANGER`) e `FairnessComplianceHub.tsx:212,213` (`#ef4444`→`CHART_DANGER`, `#eab308`→`CHART_WARNING`). Verificado: `grep -E '#[0-9A-Fa-f]{6}'` nos 2 arquivos retorna 0 ocorrências. Outros ~10 componentes que usam Recharts no projeto ficam como candidatos naturais à mesma migração em tasks futuras (não escopo do audit Configurações).
  - **T2 — sintaxe inválida do tracker (canonical-fix Fase 4)**: Reconstituída a linha `plataforma-lia/src/hooks/shared/use-teams-tab-tracker.ts:22`. O texto `const DEFAULT_(prolongedStayMs ?? DEFAULT_PROLONGED_STAY_MS) = 3 * 60 * 1000` era um typo onde a referência interna do `setTimeout` na linha 102 vazou pro nome da constante. Restaurado para `const DEFAULT_PROLONGED_STAY_MS = 3 * 60 * 1000`. Com isso o erro de baseline mencionado nas tasks #897/#918 está finalmente resolvido — `tsc --noEmit` deixa de reportar esse arquivo.
  - **T3 — duplicata `useGlobalSearchSettings` em settings/ (canonical-fix Fase 5)**: Deletado `plataforma-lia/src/components/settings/useGlobalSearchSettings.ts` (205 LoC, totalmente diferente do canonical de 87 LoC em `src/hooks/search/useGlobalSearchSettings.ts`). Verificado: 0 importadores em todo o `src/`. Era pure dead code identificado como achado lateral na #918.
  - **T3-bonus — duplicata `use-recruitment-stages` top-level**: Deletado `plataforma-lia/src/hooks/use-recruitment-stages.ts` (byte-idêntico ao canonical em `src/hooks/recruitment/use-recruitment-stages.ts`, confirmado por `diff -q` exit 0). Os 4 importadores reais (`useEditJob`, `bulk-action-modal`, `useKanbanPageSetup`, `useKanbanPageCore`) já apontam para o subfolder canônico. Top-level era órfão completo.
  - **T4 — restaurar test suite verde + comentários cosméticos**: Suite estava 3/22 passando devido à regressão herdada da Task #904 — `settings-page-enhanced.tsx:38` passou a chamar `useTranslations("settings.loading")` direto, mas o test file não mockava `next-intl`. Adicionado mock canônico (alinhado com `FunilDeTalentosClient.test.tsx` e `CandidatesTableArea.test.tsx`): `vi.mock("next-intl", () => ({ useTranslations: () => (k, p) => p ? `${k}:${JSON.stringify(p)}` : k, useLocale: () => "pt-BR" }))`. Resultado: **22/22 passando** (Duration 7.13s). Também atualizados o JSDoc na linha 9 e o header de seção na linha 209 de "8 hubs" para "10 hubs"; as asserções (`expect(allMenuButtons).toHaveLength(10)`) já estavam corretas desde a #900 — só os comentários estavam desatualizados. **Drift positivo do plano original**: o session plan previa só atualização de comentários (acreditava-se que o test estava verde); a investigação revelou e corrigiu uma regressão silenciosa na cobertura.
  - **T5 — Pipeline em 2 fontes (D-2 do audit, canonical-fix Fase 1+2 + feature-impact)**: Investigação concluída. Os endpoints `/api/v1/recruitment-stages/company-pipeline` e `/api/v1/company/pipeline-templates` são **semanticamente distintos**, não duplicatas: (a) **`company-pipeline`** representa o ESTADO atual do funil aplicado na empresa (CRUD direto: GET retorna stages ativas, PUT salva configuração); consumidores: `useCompanyPipeline` (canonical), `useRecruitmentPipeline` (settings), `useRecruitmentPersistence` (settings), `use-recruitment-stages` (subfolder), `use-recruitment-stages` (legacy top-level — agora deletado em T3-bonus). (b) **`company/pipeline-templates`** representa a BIBLIOTECA de templates reutilizáveis, com endpoints adicionais para `seed-defaults`, `clone`, `increment-usage`; único consumidor produtivo: `useEditJob.ts` (na modal de criar/editar vaga). Conclusão: nenhuma consolidação cabível — são domínios distintos. Risco de divergência mencionado no audit Dim 1 fica formalmente desclassificado. Convenção canonical: na criação/edição de vaga, ler templates de `pipeline-templates`, persistir o estágio escolhido como `pipeline_template_id` no job; o `company-pipeline` continua como override por empresa para configurações fora do escopo de templates.
  - **T6 — `credentials:'include'` (D-4 do audit, canonical-fix segurança)**: Avaliação concluída. Todos os 28 fetches em `src/components/settings/` apontam para `/api/backend-proxy/*`, que é mesma origem do frontend. O default do browser é `same-origin` — cookies (`workos_session`, `lia_access_token`) viajam automaticamente. O `proxy-handler.ts` então extrai o token via `getAuthHeaders(request)` e injeta `Authorization: Bearer <token>` no fetch para o FastAPI. Portanto: **não há vulnerabilidade real** — o status 🟡 da Dim 13 do audit fica desclassificado para ✅. As 2 instâncias de `credentials:'include'` em `FairnessComplianceHub.tsx:75,76` foram **mantidas** como excesso defensivo aceitável (custo zero, robustez extra caso o backend-proxy migre para outro domínio no futuro). Não foi criado wrapper `fetchSettingsApi` porque convergir 27 fetches num wrapper seria mexer por mexer sem ganho funcional. **Convenção canônica documentada**: nunca chamar `BACKEND_URL` direto do client (sempre via `/api/backend-proxy/`); confiar no default `same-origin`; só setar `credentials:'include'` quando o destino for cross-origin comprovado. Adicionar essa convenção como item 8 das "Convenções obrigatórias para hubs novos" na entrada da Task #903.
  - **T6 follow-up — promoção do `apiFetch` em `settings/` (Task #928 housekeeping)**: A política foi revisitada em #927 com a criação do wrapper canônico `src/lib/api/api-fetch.ts` (`apiFetch(input, init)` que aplica `credentials: 'include'` por default, preservando overrides). Como o wrapper agora existe, os 5 fetches que ainda chamavam `fetch()` cru no menu Configurações foram migrados para `apiFetch` por simetria com os outros 27 (defesa em profundidade + convenção única, baixo custo): `smart-import/useSmartImport.ts:70,131,162` (preview/import/template-download), `governance/PolicyEnginePanel.tsx:116` (apply-sector), `communication-hub/ABTestingTab.tsx:59,73,137` (mesmo passando por `BACKEND_URL` direto — melhor errar do lado seguro caso o backend migre para subdomínio), `benefits/BenefitsListSection.tsx:172` (CRUD de benefit), `benefits/useBenefitsTab.ts:249` (mesmo CRUD via aba). Resultado: 0 fetches crus em `src/components/settings/`. Decisão original da linha acima continua válida — a vulnerabilidade nunca existiu — mas adotar `apiFetch` uniformemente protege contra regressão silenciosa caso algum endpoint do menu vire cross-origin (rota separada de proxy, deploy multi-domínio, mobile webview).
  - **Bonus 1 — i18n keys com `.` (regressão da Task #919 webhooks, bloqueante)**: `messages/pt-BR.json` e `messages/en.json` linhas 6275-6282 tinham 6 chaves `agent.execution.completed`, `agent.execution.failed`, `agent.deployment.created`, `agent.deployment.paused`, `agent.approval.requested`, `agent.approval.reviewed` em `settings.webhooks.eventLabels`. `next-intl` proíbe `.` em keys (caractere reservado para nesting) e levantava `INVALID_KEY` no `LocaleLayout` server-side, **bloqueando o render inteiro de `/configuracoes`** (a página ficava em spinner infinito). Corrigido: keys renomeadas para `agent_execution_completed`, etc (12 strings em 2 arquivos), e o consumidor `WebhooksManager.tsx:30-41` ganhou normalização `event.replace(/\./g, "_")` antes do lookup — preserva os event types do backend (`agent.execution.completed`) como contrato externo, mas converte para o formato válido do next-intl no client. **Drift positivo**: sem esse fix, T7 ("validar dev-server carrega `/configuracoes`") era impossível.
  - **Bonus 2 — `voice_stream.py:93` import faltando (baseline pré-existente, bloqueante)**: `lia-agent-system/app/api/v1/voice_stream.py` importava `APIRouter, HTTPException, Query, Request, WebSocket, WebSocketDisconnect` mas usava `Depends(get_current_user_or_demo)` na linha 93 sem ter o import — `NameError: name 'Depends' is not defined` derrubava o backend inteiro no startup, bloqueando autenticação dev e os 5 hubs do menu Configurações. Adicionado `Depends` ao bloco de import. Não relacionado às mudanças do audit Configurações, mas bloqueava T7. Causa-raiz mais provável: import limpo automático de IDE em alguma task anterior.
  - **Resultado final do audit 28/abr/2026**: 10/10 recomendações P0/P1/P2/P3 endereçadas (Fase 2 + 2.5). 4 Dimensões com status 🔴 ou 🟡 do audit (1, 3, 7, 13) agora todas ✅. Frontend `/configuracoes` carrega limpo em dev; vitest do menu 22/22 verde; `tsc --noEmit` sem erros nos arquivos tocados. Pendências remanescentes catalogadas mas fora do escopo do audit Configurações: (a) decisão de produto sobre destino do bloco Workforce/Goals (Task #906 PROPOSED), (b) 68 snapshots visuais quebrados pré-existentes em outros componentes (Task #912 PROPOSED), (c) a11y E2E real com axe-core (Task #913 PROPOSED) — todas pendentes de aprovação do usuário e não bloqueiam o fechamento da auditoria.
- **Tenant Minimum Config Spec (Task #816)**: `docs/governance/tenant-minimum-config.md` é o contrato canônico (PT-BR) que define o "mínimo viável" de configuração para qualquer tenant LIA. Mapeia as 8 hints do `PreConditionChecker` → tabela alvo → query SQL → severity → cobertura pela #813; documenta que apenas `missing_company_id` (warning) bloqueia rota hoje, as 5 demais hints onboarding são `info` e só ruído. Define defaults canônicos por tabela (com marcador `[FAIR]` para os que exigem `FairnessGuard.check()` pré-persistência), matriz de seeding AUTO/ONBOARDING/MANUAL, cross-reference com WeDO Talent Guide v3.3 e skill lia-compliance, e enumera 7 próximas tarefas habilitadas (generalizar `seed_demo_company_settings` para qualquer tenant, wizard de onboarding, endpoint `/admin/tenants/{id}/bootstrap-defaults`, fechar gaps `incomplete_company_profile`/`company_website_missing` no demo, etc.). Spec-only: nenhuma mudança de código nesta task.

# External Dependencies
- Anthropic (Claude API)
- WorkOS
- Google Gemini (API, Live Audio API)
- Pearch AI
- Microsoft Graph
- Gupy ATS
- Pandapé ATS
- Merge (ATS connector)
- HubSpot
- Stripe
- Apify
- Mailgun (primary email)
- Resend (email fallback)
- PostgreSQL
- Redis
- Elasticsearch
- Sentry (error monitoring)
- OpenAI (Whisper, TTS — PSTN fallback only)
- Twilio (Voice — PSTN fallback only)
- Deepgram (STT/transcrição de voz)
- Celery