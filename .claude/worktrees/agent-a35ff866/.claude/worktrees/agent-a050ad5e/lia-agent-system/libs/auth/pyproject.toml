[project]
name = "lia-auth"
version = "0.1.0"
description = "LIA authentication interface — WorkOS + secrets provider"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
