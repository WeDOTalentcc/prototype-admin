[project]
name = "lia-services"
version = "0.1.0"
description = "LIA shared repositories and data access layer"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
