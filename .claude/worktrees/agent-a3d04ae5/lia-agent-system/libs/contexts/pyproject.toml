[project]
name = "lia-contexts"
version = "0.1.0"
description = "LIA agent system prompts and stage context definitions"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
