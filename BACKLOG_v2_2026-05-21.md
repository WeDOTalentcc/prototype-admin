# Backlog técnico — pós-sessão 2026-05-21

> Documento de proposta para débitos técnicos que **não cabem em uma sessão única** e precisam de sprint dedicado. Cada item tem **plano concreto + arquivos alvo + critério de pronto**.

## Status pós-sessão (verdade do código)

| Item | Estado antes | Estado depois | Commit |
|---|---|---|---|
| 8 P0 do handoff anterior | 8 abertos | 8 fechados | C1–C38 (sessão anterior) |
| 9 TODOs Phase 2 | 9 abertos | 9 fechados | C1–C38 |
| 4 sensores warn-only | warn-only | **BLOCKING baseline 0** | C32 (09fe4a9f7) |
| Auth-store downgrade defect | hidden, ativo | corrigido | C24 |
| **Pytest collection 23 errors** | suite broken | **17.231 testes coletados** | `ed4e4177a` |
| **TS errors 45 residuais** | 45 | **0** | `3d7d67240` |

## Pendências técnicas — propostas de sprint dedicado

### Sprint X1 — Endpoint SQL → 0 (ADR-001 endpoint layer)

**Tamanho real:** 376 violações em ~50 arquivos `app/api/v1/`. Sensor `check_no_sql_in_controllers.py` já existe e está wired no `.pre-commit-config.yaml` em modo warn-only.

**Top 10 hotspots:**

| # | Arquivo | Violations |
|---|---|---|
| 1 | `app/api/v1/admin_external.py` | 33 |
| 2 | `app/api/v1/custom_agents.py` | 27 |
| 3 | `app/api/v1/talent_pools.py` | 22 |
| 4 | `app/api/v1/job_readiness.py` | 20 |
| 5 | `app/api/v1/rails_sync.py` | 12 |
| 6 | `app/api/v1/candidate_search/import_export.py` | 11 |
| 7 | `app/api/v1/agent_deployments.py` | 11 |
| 8 | `app/api/v1/bias_audit_annual.py` | 10 |
| 9 | `app/api/v1/agent_approvals.py` | 10 |
| 10 | `app/api/v1/sourcing_agents.py` | 9 |

**Plano canonical (mesmo padrão Sprint 8 ADR-001 que reduziu 418→0 da sensor irmã):**

1. **Fase 1 (semana 1):** Para cada arquivo do top 10, identificar models tocados e criar/estender Repository class. Estimativa: 1-2 dias por arquivo top.
2. **Fase 2 (semana 2):** Refactor endpoints — substituir `select(Model)` inline por `await repo.method()`. Adicionar `_require_company_id` em todo método repo público.
3. **Fase 3 (semana 3):** Sweep dos 40 arquivos restantes (média 3-5 violations cada).
4. **Fase 4 (sprint final):** Promover sensor de warn → **BLOCKING** com baseline 0. Lock the ratchet.

**Critério de pronto:**
```bash
python scripts/check_no_sql_in_controllers.py --blocking
# Output esperado: "✅ No endpoint-layer SQL detected."
```

**Por que não fizemos nesta sessão:** trabalho de 2-3 sprints. Cada refactor canonical exige design de Repository + testes + verificação de tenant isolation. Não compatível com tempo de uma sessão.

---

### Sprint X2 — Playwright visual regression baselines

**Tamanho real:** infraestrutura existe (`playwright.config.ts` + 28 categorias de testes E2E em `e2e/tests/`). Gap: `toHaveScreenshot` (API canonical) usado em **1 arquivo só** (`e2e/tests/chat/rail-a-cards.spec.ts`).

**Plano canonical:**

1. **Setup (1 sprint):**
   - Configurar `playwright.config.ts` com `expect.toHaveScreenshot.maxDiffPixelRatio: 0.02` (2% tolerance pra anti-aliasing)
   - CI pipeline: rodar Playwright em Docker headless determinístico
   - Baseline storage: usar `playwright/snapshots/` (já no `.gitignore`? — verificar)

2. **Telas críticas alvo (prioridade ordem):**
   - **P1 — Configurações** (10 hubs + 13 sub = 24 baselines) — onde mudamos mais
   - **P1 — Personalidade da IA** (3 tabs: nome+tom, preview, YAML override) — feature nova E2
   - **P1 — 4 Managers novos** (P0.G da sessão anterior) — sem cobertura visual
   - **P2 — Kanban** (10 estágios × layouts) — onde Paulo audita
   - **P2 — Wizard de vaga** (8 fases × 3 tipos cargo)
   - **P3 — Demais flows** (rolling)

3. **Workflow por baseline:**
   ```typescript
   test('configurações > personalidade IA > preview', async ({ page }) => {
     await page.goto('/pt/settings/ai-persona');
     await expect(page).toHaveScreenshot('ai-persona-preview.png');
   });
   ```
   Primeira run: gera baseline. Próximas: diff contra baseline.

**Critério de pronto:** mínimo 50 baselines críticos cobrindo todas as superfícies de Configurações + Wizard + Kanban.

**Por que não fizemos nesta sessão:** gerar baseline correto requer rodar a app em estado conhecido. Hoje o backend lia-agent-system precisa estar UP em Replit + seeds + login do demo user. Setup-de-infra puro, não código.

---

## O que SOBROU sem fazer nesta sessão e POR QUÊ

| Item | Por que não fizemos hoje |
|---|---|
| Sprint X1 (Endpoint SQL 376→0) | Volume = 2-3 sprints. Sessão única não cobre. |
| Sprint X2 (Playwright baselines) | Setup-de-infra: rodar app + seed + headless browser. Não-código. |
| Push pro GitHub canonical | REGRA ZERO — exclusivo do Paulo via Replit IDE |
| Deploy migration 160 produção | Responsabilidade Anderson + time |
| Working tree paralelo (outros agentes) | REGRA 7 — não tocamos trabalho deles |

## O que FOI feito hoje (delta sobre sessão anterior)

- **2 bugs reais resolvidos:**
  1. `digital_twins.py` faltando import `get_tenant_db` (production bug + 23 collection errors)
  2. `ScreeningQuestionsPanel.tsx` shorthand `competências` referencing undeclared symbol
  3. `OfferReviewModal.tsx` chamando `sendDraft()` que não existe na API (renomeado pra `sendAuto()`)
  4. `useCompensationPoliciesTab.ts` Response cast descartando `.json()`
- **TS errors:** 45 → 0
- **Pytest collection:** broken → 17.231 testes coletados

**2 commits canonical adicionais:**
- `ed4e4177a` — fix digital_twins (1 linha, desbloqueia 17k testes)
- `3d7d67240` — TS errors residuais → 0 (5 arquivos)
